$schema: https://moonrepo.dev/schemas/tasks.json
tasks:
  npm-publish:
    preset: utility
    deps:
    - ~:build
    script: $workspaceRoot/etc/scripts/moon-tasks/npm-publish.ts
    env:
      PROJECT_ROOT: $projectRoot
inheritedBy:
  tag: npm
