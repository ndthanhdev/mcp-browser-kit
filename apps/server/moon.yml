$schema: "https://moonrepo.dev/schemas/project.json"
language: typescript
platform: node

tags:
  - tsup

tasks:
  build:
    deps:
      - tsup-build

dependsOn:
  - rpc
  - core-server
  - driven-extension-driver
