$schema: "https://moonrepo.dev/schemas/project.json"
language: typescript
toolchain:
  default: node

tags:
  - tsup

tasks:
  build:
    deps:
      - tsup-build

dependsOn:
  - core-server
  - core-utils
  - driven-extension-driver
  - driven-logger-factory
  - server-driven-extension-channel-provider
  - server-driving-trpc-controller
